{
  "name": "sloppy-sluggers",
  "version": "0.2.4",
  "private": true,
  "type": "module",
  "description": "A two-way baseball deckbuilding roguelike. No runtime dependencies.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "balance": "node scripts/balance.mjs",
    "validate": "node scripts/validate.mjs"
  }
}
